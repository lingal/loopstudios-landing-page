import AboutSection from './AboutSection';
import ProjectsSection from './ProjectsSection';

const Main = () => {
  return (
    <main className="main">
      <AboutSection />
      <ProjectsSection />
    </main>
  );
};

export default Main;
